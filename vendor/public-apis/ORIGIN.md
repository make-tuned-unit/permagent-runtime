# Origin

This directory holds the README catalog from https://github.com/public-apis/public-apis
so Permagent can browse and toggle public APIs under Settings → Data sources.
It is not a runtime dependency of those APIs; nothing is fetched until a source is enabled.
