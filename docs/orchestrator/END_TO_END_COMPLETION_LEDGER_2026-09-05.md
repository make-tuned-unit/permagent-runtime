# End-to-end completion ledger

## Unfinished portfolio retained — September 6

The orchestration repair is a prerequisite, not a replacement for the user's
unfinished work. Continue these existing masters using their exact receipts:

| Program | Recorded frontier | Next dependency |
|---|---|---|
| Coding harness | P4; bridge now 11/11, B6 fixtures 6/6, Henry regression under repair | exact Henry gate, pending-dispatch recovery, B5 live integration before routing qualification |
| Voice | V3b source/local tests; device gate unpassed | reconcile local producer receipt, fresh V6 daemon/device evidence |
| Companion | C1 boundary reliability | use shared continuity evidence; authoritative state and cross-platform journeys |
| Spectral learning | S1 contracts | resume existing-library contract/episode work; no parallel memory store |
| World | W0-W4 passed | W5 native performance and installed journey, not another browser-only 60fps claim |
| Suite polish | U1 reviewed/passed; U2 active | shared-system visual acceptance, then macOS screens, then iOS, then watchOS |

The table records scope and dependencies, not six simultaneous running workers.
Root rotates available workers among ready lanes and preserves existing final
release, privacy, permission, and bounded-spend requirements.

## September 6 orchestration incident — not resolved by a restart

Root ended a turn after worker batches completed without dispatching their
successors. The earlier handoff rule was documented but not enforced. That is
an orchestration failure; neither an active YAML status nor restarting a worker
establishes durable continuation. The host goal currently reports `usageLimited`;
this is a separate external constraint, not an explanation for failing to use
the active operator-authorized turn.

Recovery now executing: runtime bridge verification, product scheduler audit,
and shared UI implementation have explicit active owners. Additional Claude
Code Sonnet and Cursor Gemini Flash Low CLI workers were launched for bounded,
read-only harness and Spectral audits respectively. Initial sandbox login
failures were resolved by approved external execution using existing logins;
no credentials were copied. Their outputs still require review.

Routine verification gates are machine checks, not new human approval requests.
On each completion root must review evidence and dispatch the ready successor,
or record a concrete blocking dependency and use another ready lane. Permission,
release and scope boundaries remain intact. This record is not a claim that a
production scheduler or unattended host continuation has been fixed.

Resource guard audit: `df -k` reports KiB, so an 8 GiB threshold is 8,388,608,
not 16,777,216. The verifier is checking an apparent unit mismatch that falsely
reported roughly 10 GiB free as 5 GiB. No cleanup is justified by that reading.

CLI field results: Claude Code completed its bounded audit using
`claude-sonnet-5`, reporting $0.145314 list-price usage (not a verified invoice).
Its claim that B6 retry-storm coverage was absent was contradicted by root's
source check: `session_manager.rs` already contains
`b6_bounded_retry_storm_has_distinct_attempts_and_terminal_unknown`. Audit
recommendations must be reconciled with current source, not blindly scheduled.
Cursor authenticated, but the Gemini Flash Low job was refused by its account
usage limit; no task result or completion is claimed and no spend-limit change
was made. Existing workers continue within the approved scope.

## Worker handoff rule

An informational message does not restart an idle/completed worker. When a
producer finishes a dependency (for example, a compile repair), its handoff
must activate the existing verifier with `followup_task`, including the frozen
source scope and bounded next command. `send_message` alone is appropriate only
for an already-running recipient. Root independently reviews the result and
keeps ownership explicit. This is a collaboration-session rule, not proof that
Permagent's production runtime has implemented or passed durable continuation.

New suite-polish program is part of the portfolio. Research is recorded;
source inventory is active. Its ordering is macOS acceptance, then iOS, then
watchOS, with the existing reliability lanes proceeding independently.

## Subsequent runtime and continuation checkpoint

Approved, exact-scope cleanup removed only obsolete generated Cargo artifacts
for hash `permagent-34c916f5dd0f78ad`; free disk recovered to 11 GiB. Source and
user data were untouched. On the coordinated snapshot, core `cargo check`
passed and the program bridge executed nine tests: eight passed, one failed.
The failing tampered-approval-hash case remains under repair; it is not a pass
and the assertion is not to be weakened merely to advance the DAG.

Voice control/start-storm and real batch-fallback repairs reached source READY.
The sole runtime worker owns daemon compilation and focused test execution;
the continuity worker owns the remaining approval/receipt races. Root owns
independent review and immediately assigns the next dependency-ready work at
worker completion. Public, checksum-verified local speech fixtures are staged
for an opt-in real-model test; production voice is not enabled or accepted by
that staging step. All five program masters remain incomplete.

## Full UI regression checkpoint — September 5, 23:19 local

The first full suite found 7 failures (1,879 passed): new chart/referrer and
terminal text bypassed the existing type ramp, a new referrer button bypassed
the common Button primitive, and the finance currency fixture omitted the
store selector required by the existing liveness binding. Root corrected the
production usages without increasing design-system exemption budgets, repaired
the fixture, and added chart/terminal type-ramp assertions.

Changed-snapshot full rerun: **250 files passed, 1,887 tests passed, zero
failed**, 14.46 seconds. TypeScript passed. World independently passed actual
walking, selection, station glide, reduced-motion/hidden-view and browser 60fps
gates; W3 and W4 are passed, W5 still awaits native release acceptance.

Rust verification remains unpassed: latest bridge compile failed before test
execution, and free disk reached 4.5 GiB (below the 8 GiB floor). Exact stale
generated-artifact cleanup candidates are under read-only audit; nothing has
been deleted. Voice optional streaming adapter and route integration are under
review for speaker admission, cancellation, fallback and bounded worker count;
no live online-model or phone success is claimed.

## Latest continuation checkpoint — September 5, 23:00 local

- Isolated B5 HTTP gate executed successfully on temporary port 3099: health,
  authenticated projection/update/history, unauthenticated refusal and malformed
  update refusal. Temporary daemon stopped; production remains untouched.
- Automatic registered-program completion wiring is **not yet trusted**.
  Review found missing approval provenance, preflight/transaction races, and
  synthetic per-gate success derived from a generic completion verdict. Bridge
  owner is repairing these with adversarial regressions. Compilation also found
  two concrete type errors; no new bridge tests executed on that snapshot.
- Root reran all 188 World tests and the production UI build successfully.
  Independent pointer-journey rerun exposed a camera-release race in the evidence
  script; W3 stays active until corrected real-pointer checks pass. Native 60fps
  and installed-app acceptance remain separate, unpassed gates.
- Browser worker executed 143 focused no-cloud tests and typecheck, including
  navigation/media teardown and address input/referrer regressions. These are
  not a claim of fresh installed native YouTube or real Reddit traffic evidence.
- Ownership proceeds directly: bridge trust/continuation repair; sole Cargo
  runtime owner with B6.7 and queued daemon tests; voice V3b optional streaming
  contract/fake tests; root World independent verification and integration.
  No master has been bound to runtime goals using invented approval or evidence.

## Continuation recovery — September 5, operator resume

The prior worker-idle condition was real, not background execution. Root
resumed all three existing worker lanes: bridge/runtime verification,
durable handoff audit, and device/World interaction evidence. Completion
notifications must trigger root review and the next dependency-ready task;
an active YAML node alone is never evidence of a running worker.

The external session goal reports `usageLimited`; this session cannot change
that control through goal-status APIs. Explicit-turn work continues, but no
claim is made that Permagent can bypass the hosting session's usage control.

Five existing master manifests validated successfully. Voice audit uncovered
a missing partial-STT producer, now an explicit V3b predecessor of device
acceptance rather than a misleading rebuild-only task. Root also repaired
post-quit listener observation in the deployment script: inspection errors
refuse installation instead of becoming a free-port result. Twelve shell
regressions pass; dry-run reports app-sidecar ownership. No app was stopped
or replaced by those checks.

Status: executing, not complete. This is an evidence/ownership index over existing
masters, not a new scheduler or replacement DAG. User requested closing all
unfinished work on 2026-09-05. Root owns integration; low-cost workers implement.

Latest iOS update: operator requested root/Astra personally perform the design
pass. See `IOS_DESIGN_PASS_DAG_2026-09-05.md`:150 executed passing tests, signed
Release phone build installed, launch gated by locked device. See
`IOS_LIVE_FEEDBACK_2026-09-05.md` for confirmed old-hub404 and actual
deepseek-chat vs incorrect phone label, plus5.777s server first-audio timing.
No live playback pass or all-DAG completion is implied by installation.

World expansion now has `WORLD_BLENDER_MASTER_PROGRAM_DAG.yaml` and its linked
receipt. Root/Astra owns Blender art; cheaper worker preserved visual transforms
while batching repeated static furniture. Twelve authored character assets bind
to the existing live skeleton; no parallel behavior system. Preliminary Chrome
samples:60fps,274draws,256025triangles,DPR1; native release qualification pending.

## Reconciliation

An active manifest does not prove a live worker. Completed audits are not
implemented features. Compiled tests are not executed tests; device installation
is not a successful journey. Source receipts do not establish installed build
freshness. Preserve old evidence as historical and append current results.

| Lane | Actual owner at dispatch | Next work | Exit before successor |
|---|---|---|---|
| Harness B5.4 | close_b5_ui / GPT-5.6 Luna | canonical budget parser, live events, hydration/reconnect, UI and regressions | focused UI suite + typecheck + root review |
| Harness B5.2/B5.3 runtime | close_runtime_gate / GPT-5.6 Luna | recover specific test execution conditions and run targeted runtime assertions | actual executed tests or precise unresolved blocker |
| Device/browser | root | recover simulator access, build freshness, voice/watch/browser journey evidence | exact source/build and runtime evidence; physical mic still separate |
| Spectral S1 | pending next free worker slot | turn reviewed S0 findings into existing-API contract and synthetic regressions | no parallel substrate; consent/observation/identity agreement |

Browser/referrer worker `referrer_build_browser_ux` was resumed for its existing
paths and focused verification; it does not own the Rust build slot or B5 UI
store paths. C0's declared audit exit was reconciled by root and marked passed;
the existing `target/debug/permagent-eval program validate --manifest
docs/orchestrator/PERMAGENT_COMPANION_MASTER_PROGRAM_DAG.yaml` exited 0 and
reports C1 ready, complete false. This is topology/audit evidence only.

B5.4 worker's 68 passing focused tests/typecheck are not yet promotion evidence:
root found an additional cross-session stale projection identity issue and
requested regression/repair. A malformed run B must never display run A's
last-known spend as if it belongs to B. Temporal replay ordering also needs to
be scoped to the relevant session, not reject unrelated sessions by timestamp.

## Dependency order

1. Close B5.4 and B5 runtime gates in parallel, fan in at B5.5 integrated
   event-to-UI proof. Independent device readiness checks may overlap.
2. Execute existing B6 bounded fake-provider faults. Promote P4 only after its
   actual exit receipts; move into existing P5 routing, recovery/multiworker,
   held-out and release nodes in declared order.
3. Close voice V6 and analytics/browser live gates against fresh artifacts.
   Do not claim real Reddit referrer detail if the browser only sends an origin.
4. Companion C0: reconcile audit-owned requirements separately from downstream
   runtime/device gates. A dirty worktree requires path ownership and a diff
   identity, not erasure or an impossible clean-tree precondition. A baseline can
   honestly record not-run measurements without pretending later tests passed.
5. Review Spectral S0, then dispatch S1-S7 sequentially with existing cross-program
   ownership. C3/C4/C6 consume shared receipts rather than duplicate memory,
   routing or trust implementation.
6. Final fan-in requires all inherited release/rollback and macOS/iOS/watchOS
   supported journeys. Remaining hardware, spend or permission decisions are
   explicit; they cannot be silently treated as passes.

## Resources and verification discipline

One Rust compile/link owner at a time. Coordinate Xcode linking with that owner;
parallel read-only work and focused UI tests remain useful. No broad repeated
suite, no continuous polling, no repeated unchanged failure. Preserve regression
tests for every production change. Review both positive and adversarial cases.
Worker completion triggers review and next dispatch; tool/thread limits require
root continuation rather than reporting an idle plan as ongoing execution.

## Current recovered device evidence

On 2026-09-05, sandboxed `xcrun simctl list devices booted` failed with denied
CoreSimulator logging/XPC. The approved escalated invocation succeeded. Escalated
`list devices available` shows iPhone 17 Pro A82A2157-CFC5-41B9-91A7-611E5B75C7C9
and iOS/watchOS 26.5 devices available, all shutdown. This removes the claim that
simulator discovery is inherently unavailable; boot/test is not yet proven.
Disk reports 25 GiB available. Previous temporary test artifacts at
`/private/tmp/permagent-v4-derived-20260904/Build/Products` are absent, so old
test-without-building commands cannot be reused as-is.

No release or global completion is claimed by this ledger.

## Runtime recovery and continued execution

The daemon SIGKILL was diagnosed from the exact crash report as CODESIGNING
Invalid Page; only the generated ONNX dylib was invalid. Narrow re-signing
restored execution: seven projection tests passed, zero failed. The core
serialization assertion also executed and passed (its old shell-wrapper error
is retained separately). Runtime worker now owns B6 combined fault fixtures.

B5 UI cross-session regression repairs now have 70 focused passing tests and
typecheck; root/B5.5 integration remains required. Browser/referrer worker
reported 91 focused plus 17 Grow tests passed and typecheck, then was dispatched
to investigate self-referral filtering and referrer query privacy. Spectral S1
contract review found correction scope/causality and completeness defects and
was reopened before wiring or promotion.

Root improved the existing daemon verification helper with explicit `--lib`
scope, matching compile/execution targets, strict signature verification and
fail-closed missing-artifact behavior. Seven no-build Python regressions pass,
including default integration coverage, release/profile path, signing failure,
missing dylibs, Linux and rejected extra arguments. Those tests also exposed
and fixed the macOS Bash 3 empty-array/nounset failure. A real scoped runner
execution is queued with the sole Rust build owner; no parallel Cargo spawned.

Root reproduced a further render-level defect with a failing regression: when
the selected session changed, the previous session's child rollup remained
visible until the new HTTP response. The component now binds rollup evidence
to its session ID before rendering. The actual three-file B5 UI command passed
52 tests (19 costMeter, 29 livenessSync, 4 CostStatusline), and the separate
useAppNavigate.replay suite passed 19; typecheck and scoped diff check passed.
The worker's earlier 70 count included that fourth file, omitted from its
receipt command; that documentation correction was requested. Current combined
scope is 71 tests after root's new regression, not 71 additional tests.

Runtime availability is distinct from launchctl ownership: installed CLI
`permagent status` reports not loaded, but local port3001 has PID77357 listening
and `/status` returns `ok`. This is current installed-daemon health only, not
proof that its binary contains today's fixes. No running daemon was replaced.

The installed CLI's deterministic `doctor --json` confirmed authenticated
roundtrip (401 without token/200 with token), WebSocket upgrade, database
integrity and intact decision audit chain. It reports running build v1.31.0,
SHA b0ebaa689, same Spectral pin. `/ui/` returns404 and launchd plist is absent;
neither alone proves failure of a desktop-owned daemon. Root added3 regression
tests and a surgical CLI status fix distinguishing launch-agent ownership,
listener presence and unknown probe errors; execution is queued with Rust
owner. Doctor also reports both Keychain and YAML secret sources exist. No
secret contents were printed, migrated or deleted; that warning requires
careful credential-source reconciliation, not automatic credential removal.

Continuity audit is assigned to the browser worker after its source fixes:
repository references to ProgramDag/ProgramFrontier are in permagent-eval only.
The worker must prove a runtime dispatch consumer or propose a minimal bridge
to the existing scheduler. Manifest activation cannot be called unattended
worker execution without that path.

## Device and shared producer/consumer progression

A new device inventory found a paired available iPhone14ProMax. Signed Debug
build using the existing configured team and physical destination
00008120-000E5064349B401E passed, with no provisioning changes. Installation
and launch of ai.permagent.ios on device2FE03794-87D0-55F0-8B5B-70A083645009
passed. Existing data was not erased; no daemon was replaced. Device build
used `/private/tmp/permagent-e2e-tests-20260905/Build/Products/Debug-iphoneos/Permagent.app`.
Live microphone/playback/WatchConnectivity journeys still require observation.

All six B6 core fixtures now executed and passed per runtime-worker receipt.
Root consumed the shared `scripts/testdata/budget_projection_v1.json`, which
the real reopened-ledger Rust test asserts in full after normalizing IDs/time.
The actual UI event/store/render and reload hydration test renders identical
unknown hold/remaining evidence from that fixture. Four focused UI suites now
pass72 tests, zero failures; typecheck passed. This closes cross-language
contract drift for that restart fixture, not a live HTTP/installed CLI pass.

## Fresh iOS regression execution — passed

Simulator boot succeeded outside sandbox. Fresh command:

```text
xcodebuild test -project ios/PermagentMobile/PermagentMobile.xcodeproj \
  -scheme PermagentTests \
  -destination 'platform=iOS Simulator,id=A82A2157-CFC5-41B9-91A7-611E5B75C7C9' \
  -derivedDataPath /private/tmp/permagent-e2e-tests-20260905 \
  -resultBundlePath /private/tmp/permagent-e2e-tests-20260905.xcresult \
  -jobs 2 CODE_SIGNING_ALLOWED=NO -quiet
```

Exit 0. `xcrun xcresulttool get test-results summary --path
/private/tmp/permagent-e2e-tests-20260905.xcresult` confirms 130 total, 130 passed,
0 failed, 0 skipped; iPhone 17 Pro arm64 iOS26.5 build23F77. This is actual
simulator regression execution, not a compile-only result. Hosted-app-free scheme
does not establish full app voice/microphone or physical watch behavior.
`python3 scripts/test_voice_latency_report.py` also exited0. V6 remains incomplete
until its remaining exact-build/live journeys pass.

Fresh `xcodebuild build` with scheme `Permagent`, the same destination and
derived-data directory, `-jobs 2 CODE_SIGNING_ALLOWED=NO -quiet` exited 0,
including the embedded watch app. `simctl install` and `simctl launch` for
`ai.permagent.ios` both succeeded (launch PID 34652). Screenshot
`/private/tmp/permagent-e2e-launch-20260905.png` shows the real pairing onboarding,
not the chat UI. This simulator has no hub pairing; microphone, reply playback,
word highlighting, next-turn model switching and WatchConnectivity remain
unverified. Build warnings about MainActor, AVCaptureSession Sendable capture,
and deprecated Bluetooth configuration remain separate review debt.

The session goal API currently reports `usageLimited`. Work in this explicit
user turn proceeds, but automatic goal continuation is not established and must
not be advertised as an unattended scheduler. Root cannot change that setting
using the goal-status tools.
