{
  "name": "permagent-desktop",
  "version": "1.31.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build:ui": "cd ../command-center && npm ci && npm run build",
    "build:daemon": "cd ../.. && cargo build --release --bin permagentd && bash scripts/sign-dev-binaries.sh permagentd",
    "build:sidecar": "scripts/copy-sidecar.sh",
    "build:cli": "cd ../.. && cargo build --release -p permagent-cli --bin permagent && bash scripts/sign-dev-binaries.sh permagent",
    "build:cli-sidecar": "scripts/copy-cli.sh",
    "build:mcp-runtime": "scripts/copy-mcp-runtime.sh",
    "build:whisper-model": "scripts/copy-whisper-model.sh",
    "build:icons": "cd ../.. && bash scripts/generate_icons.sh",
    "build:all": "npm run build:icons && npm run build:ui && npm run build:daemon && npm run build:sidecar && npm run build:cli && npm run build:cli-sidecar && npm run build:mcp-runtime && npm run build:audiocap && npm run build:whisper-model && npm run tauri build",
    "build:all:local": "npm run build:icons && npm run build:ui && npm run build:daemon && npm run build:sidecar && npm run build:cli && npm run build:cli-sidecar && npm run build:mcp-runtime && npm run build:audiocap && npm run build:whisper-model && npm run build:local",
    "build": "npm run tauri build",
    "build:local": "tauri build --config '{\"bundle\":{\"createUpdaterArtifacts\":false}}'",
    "build:audiocap": "scripts/build-audiocap.sh"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
