# Build artifacts
target/

# Sidecar binaries and dylibs (built, not committed — 200MB+)
binaries/permagentd-*
binaries/permagent-*
binaries/*.dylib

# Bundled MCP runtime: Node binary + web-search server packages (built by
# scripts/copy-mcp-runtime.sh, not committed — ~140MB) (#358)
resources/mcp-runtime/

# Bundled Whisper dictation model (fetched by scripts/copy-whisper-model.sh,
# not committed — ~78MB)
resources/whisper/

# Node
node_modules/
