// MouthChunk — the lazy aperture chunk (THE_CAVE_vision_bible.md §9 W1).
// The distant Mouth sightline geometry, far above the dome. Geometry only — W4
// owns the daylight. Default export for React.lazy in CaveSystem.tsx.

import { MouthAperture } from './MouthAperture';

export default function MouthChunk() {
  return <MouthAperture />;
}
