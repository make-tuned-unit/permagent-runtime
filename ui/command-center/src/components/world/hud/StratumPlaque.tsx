// StratumPlaque — placeholder during the walkable-cave rebuild.
//
// This used to be an HTML overlay bound to the descent rail's active stop index
// (knob 1: real date-range + counts for the framed chamber). The rail is gone —
// the cave is walked now, not railed — so the plaque is being repositioned to sit
// IN each chamber as a walk-up plaque/pedestal (rebuild step §6). Until that lands
// this renders nothing. The real date-range + count data still lives on each
// StratumDef (deriveStrata) and will drive the in-room plaque.

export function StratumPlaque() {
  return null;
}
